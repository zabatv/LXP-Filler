services:
  - type: web
    name: lxp-journal-filler
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: .venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
